{
  "version": 1,
  "description": "Copilot Omni session lifecycle hook — pure Python, stdlib only, corporate-safe",
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "command": "python \"${OMNI_PLUGIN_ROOT}/hooks/session_start.py\"",
        "timeout": 10
      }
    ]
  }
}
