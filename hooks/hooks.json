{
  "version": 1,
  "description": "Copilot Omni lifecycle hooks — pure Python stdlib, corporate-safe",
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "comment": "Session banner + policy permission checks + startup metrics",
        "bash": "python \"${COPILOT_PLUGIN_ROOT}/hooks/session_start.py\"",
        "timeoutSec": 10
      }
    ],
    "preToolUse": [
      {
        "type": "command",
        "comment": "Policy guard — shlex-safe command parse, protected-path enforcement",
        "bash": "python \"${COPILOT_PLUGIN_ROOT}/hooks/pre_tool_use.py\"",
        "timeoutSec": 8
      }
    ]
  }
}
