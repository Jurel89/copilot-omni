---
name: omni-status
description: Show current Copilot Omni project config and active runs.
---

# /omni-status

Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/omni.py" status`.

Prints:
- `.omni/config.json` contents
- List of run directories in `.omni/runs/`
