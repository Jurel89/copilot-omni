---
name: omni-list
description: List all skills, agents, and commands this plugin provides.
---

# /omni-list

Run `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/omni.py" list all`.

Prints every discoverable skill (with description), every agent, and every slash command.
