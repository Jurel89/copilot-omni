---
name: omni-memory
description: Manage project memory — search, capture, export, prune.
---

# /omni-memory

Use the `remember` skill plus the MCP tools `memory_capture`, `memory_search`, `memory_export`, `memory_prune` from the `copilot-omni` MCP server to manage project-level durable knowledge.
