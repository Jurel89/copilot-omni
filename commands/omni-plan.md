---
name: omni-plan
description: Run planning-only workflow (discuss, spec, plan, review) without executing.
---

# /omni-plan

Use the `plan` skill to produce a structured plan artifact in `.omni/plans/` without executing any code changes. Ideal when you want to review the approach before committing to implementation.

If the repo is not initialized, run `/omni-init` first.
